## Setup

Make sure to install the dependencies:

```bash
pnpm install
```

## Development Server

Start the development server on `http://localhost:3000`:

```bash
pnpm run dev
```

## Production

Build the application for production:

```bash
pnpm run generate
```

Locally preview production build:

```bash
pnpm run preview
```


<!-- 'primary'
'secondary'
'success'
'warning'
'error'
'info'
'neutral'
fnm use  20 
-->



先查看当前项目的目录结构，只分析 app 目录，告诉我这是一个什么类型的管理后台项目

请重点学习 app/pages, app/components, app/api这三个目录    